NULL =

# for now we only have the application icon in 24x24 (which is needed
# in the main menu to avoid a blurry icon)

icondir = $(datadir)/icons/mate/24x24/apps
icon_DATA = 				\
	mate-disk.png			\
	$(NULL)

EXTRA_DIST = \
	$(icon_DATA)

clean-local :
	rm -f *~
